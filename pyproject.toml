[project]
name = "p8-scoring-api"
version = "1.0.0"
description = "API de scoring credit - deploiement, CI/CD et monitoring (OpenClassrooms P8)"
readme = "README.md"
requires-python = ">=3.13"
dependencies = []

[tool.uv]
package = false